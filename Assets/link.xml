<!--
  WHY THIS FILE EXISTS — 2026-08-10.

  ProjectSettings has stripEngineCode: 1, so Unity registers only the native
  engine classes it can SEE referenced. UnityEngine.CapsuleCollider is never
  named by any line of managed code in this project, and it was therefore
  stripped from every player build. Measured in the shipped Xcode export
  rather than inferred:

      build/ios/Il2CppOutputProject/Source/il2cppOutput/UnityClassRegistration.cpp

  registers BoxCollider, Collider, MeshCollider and SphereCollider under
  "Physics" — and no CapsuleCollider. Device and simulator builds alike. The
  built app then said, from first launch, before any input, repeatedly:

      Can't add component because class 'CapsuleCollider' doesn't exist!

  IT IS REQUESTED BECAUSE OF A UNITY QUIRK. GameObject.CreatePrimitive(
  PrimitiveType.Cylinder) attaches a CAPSULECOLLIDER — the cylinder is the odd
  primitive out (Cube -> Box, Sphere -> Sphere, Plane -> Mesh). So the type is
  demanded only at runtime and is invisible to a source grep, which is why this
  looked like a phantom for a day. There are 33 live CreatePrimitive(Cylinder)
  sites: PartVisualFactory (25), ArenaHazards (6), BuilderManager (2).

  MOST OF THOSE 33 ARE HARMLESS NOISE *TO THE SIMULATION* — PartVisualFactory.Deco
  and the decorative ArenaHazards.Prim(..., collider: false) sites destroy the
  collider on the next line, and with the class stripped GetComponent returns
  null and Destroy(null) is a no-op, so the end state is identical.

  ⚠ BUT THE NOISE IS NOT INVISIBLE, AND THIS IS THE PART THAT WAS GOT WRONG.
  It was reasoned — from boot.config carrying player-connection lines on one
  build and not the other — that Unity's on-screen console is a DEVELOPMENT
  BUILD feature, and therefore that a release build would log quietly and no
  tester would ever see this. THAT IS FALSE ON THIS il2cpp TARGET. Measured
  2026-08-10 against the RELEASE build's installed bundle — boot.config with
  ZERO player-connection lines, confirmed by process lineage and Editor.log —
  the console appeared anyway: it is ERROR-TRIGGERED, not flag-gated.

  So without this file, EVERY TESTER'S FIRST LAUNCH SHOWS RED ERROR TEXT OVER
  THE PRODUCT, on any configuration, before they touch anything. That is the
  difference between a clean first launch and a screen of red, and it is
  recorded here precisely because the flag-gated assumption is the reasonable
  one and the next person will make it too.

  ⚠ AND IT INVERTS A GUARANTEE: "clean screen" now depends on the code logging
  no errors rather than on a build setting. Any NEW error introduced anywhere
  puts the console back over the product without a single build flag changing —
  which is why a store capture must be checked on the FINAL build, immediately
  before capturing, rather than inherited from an earlier verification.

  ⚠ BUT DO NOT OVER-READ THE ABOVE EITHER — IT IS MEASURED ON THE SIMULATOR.
  Every observation here comes from a Simulator-SDK player. A second one landed
  after this note was written: the [dev] status line, gated on
  Debug.isDebugBuild (MobileBuilderUI.cs:3161), ALSO renders on the flag-clean
  release simulator build — so isDebugBuild is TRUE there, and that file's own
  comment at :3154 ("false in a release build, so a shipped player…") describes
  something the simulator does not honour.
  The economical explanation is that Unity ships ONE dev-flavoured SIMULATOR
  runtime per architecture — Trampoline carries -sim-arm64 / -sim-x64 variants,
  i.e. an ARCH split, with no dev/release split beside it. If that holds, the
  console-on-release and [dev]-on-release are ONE platform fact rather than two,
  and BOTH are properties of the simulator runtime.
  So the honest scope is: on the SIMULATOR the console is error-triggered
  regardless of the flag. WHETHER A DEVICE RELEASE BUILD BEHAVES THE SAME IS
  UNTESTED — no device build has ever been made from this code. It is on owen's
  first-device-build checklist. Preserving CapsuleCollider is worth doing either
  way, because the hollow yard pillars are a device defect independent of who
  can see the log; this paragraph exists so the NEXT reader does not inherit a
  simulator measurement as a device fact — which is the mistake this whole note
  is a record of, made once already in the other direction.

  ONE IS NOT NOISE, AND IT IS WHY THIS FILE IS A FIX RATHER THAN A MUTE:

      ArenaHazards.cs, PillarHazard.Build() — Prim(..., collider: true)
      "Static pillar: an obstacle, never a damager. Real collider."

  It is the only cylinder in the project that KEEPS its collider. Without this
  preservation the yard's two pillars are created with no collider at all and
  robots drive straight through them — silently, with no exception beyond the
  spam above. The yard is L1 "Scrapyard Open", the FIRST league in the game, and
  ArenaHazards.Summary("yard") returns the literal string "pillars", which the
  league card renders: the app advertised an obstacle it did not have, in the
  first fights every new player sees.

  WHY NO BENCH CAUGHT IT: HazardBench is 23/23 green in the EDITOR, and the
  editor does not strip engine code. This class of defect is invisible to every
  bench in this project by construction — it can only be seen in a built player.

  WHY NOT AN EXPLICIT AddComponent<CapsuleCollider>() IN PillarHazard: it was
  tried first and measured, and it is worse on two counts. Unity's default
  capsule is height 1 while CreatePrimitive's is height 2, so the pillar would
  silently get a collider 60% of its height; and it requires collider: false,
  whose Object.Destroy is deferred in play mode and a no-op in edit mode,
  leaving two overlapping capsules. The numbers are in PillarHazard's comment.

  HOW TO VERIFY THIS IS WORKING: after a build, that generated
  UnityClassRegistration.cpp must contain

      RegisterUnityClass<CapsuleCollider>("Physics");

  If it does not, this file is not being picked up and the pillars are hollow
  again. That check is the whole point; a green editor bench cannot replace it.

  SCOPE: deliberately one type. This is not a blanket "preserve physics" — the
  other collider types are named in managed code and survive on their own, and
  a broad preserve would hide the next instance of exactly this bug.
-->
<linker>
  <assembly fullname="UnityEngine.PhysicsModule">
    <type fullname="UnityEngine.CapsuleCollider" preserve="all" />
  </assembly>
</linker>
